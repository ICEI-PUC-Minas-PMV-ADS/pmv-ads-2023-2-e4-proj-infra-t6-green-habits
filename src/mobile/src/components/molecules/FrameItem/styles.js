import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  frame: { gap: 8 },
  frame__container: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginTop: 12
  },
})

export default styles
