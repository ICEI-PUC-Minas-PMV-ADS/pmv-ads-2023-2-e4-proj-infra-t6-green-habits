import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontStyle: 'normal',
    fontWeight: '500',
    color: '#000706',
    marginBottom: 8
  },
})

export default styles
